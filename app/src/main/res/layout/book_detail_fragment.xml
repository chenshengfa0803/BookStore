<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include
            android:id="@+id/detail_toolbar"
            layout="@layout/toolbar" />

        <com.bookstore.bookdetail.BookDetailListView
            android:id="@+id/book_detail_list_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"
            android:listSelector="#00000000"
            android:paddingEnd="@dimen/detail_listview_horizontal_padding"
            android:paddingStart="@dimen/detail_listview_horizontal_padding" />
    </LinearLayout>

</FrameLayout>